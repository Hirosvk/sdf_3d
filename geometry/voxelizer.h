namespace Geometry {
  class Voxelizer {
/*
 *  Mesher(x_bounds, y_bounds, z_bounds, SignedDistanceFunction&)
 *    generateVoxels();
 *    voxels
 *
 *    generateVertices();
 *    vertices
 *
 *    generateCubes();
 *      findVertex(voxels);
 *    cubes
 *
 *  Voxel
 *    id
 *    point
 *    signedDistance
 *
 *  Vertex(Voxel voxels[]) // 2 voxels
 *    <= 2 voxels
 *
 *  Cube(Voxel voxels[]) // 8 voxels
 *    polygons
 *
 *  Polygon
 *    vertices
 *
 */


  }
}
